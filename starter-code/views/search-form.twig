<!DOCTYPE html>
<html lang="en">
  <head>
    <title>The Chuck Norris Joke Department</title>
    <link rel="stylesheet" type="css/text" href="../public/stylesheets/style.css">
  </head>
  <body>
    {#<form action="/search" method="post">
        <label for="keyword">Keyword</label>
        <input id="keywordID" name="keyword" type="text">
        <button type="submit">Send</button>
    </form>#}
    <form action="/search" method="POST">
        <label for="userInput">keyword:</label>
        <input id="keyword-input" type="text" name="userInput">
        <input type="submit" value="Submit">
    </form>

    {% if data.items.length > 0 %}
        <h2>Search Results</h2>
        {% for joke in data.items %}
        <p>{{joke.value}}</p>
        {% endfor %}
    {% endif %}
    
  </body>
</html>
