  <body>
    <h1>Categories</h1>
    <ul>
      <% categories.forEach( (category) => {%>
        <li>
          <a href="?cat=<%= category %>" ><%= category %> </a>
        </li>
      <% }) %>
    </ul>

  </body>
